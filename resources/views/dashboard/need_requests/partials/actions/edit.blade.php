@can('update', $need_request)
    <a href="{{ route('dashboard.need_requests.edit', $need_request) }}" class="btn btn-outline-primary btn-sm">
        <i class="fas fa fa-fw fa-edit"></i>
    </a>
@endcan
